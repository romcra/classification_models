from distutils.core import setup
setup(
  name = 'classification_models',
  packages = ['classification_models'], # this must be the same as the name above
  version = '0.1',
  description = 'A library that will be used to find the model that explains the variable the best',
  author = 'Rodrigo Marcos',
  author_email = 'rodri.pinar@gmail.com',
  url = 'https://github.com/romcra/classification_models', # use the URL to the github repo
  download_url = 'https://github.com/romcra/classification_models/tarball/0.1', # I'll explain this in a second
  keywords = ['datascience', 'methodselection'], # arbitrary keywords
  classifiers = [],
)
